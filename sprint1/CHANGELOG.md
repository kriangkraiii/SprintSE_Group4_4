# Project Changelog / บันทึกการเปลี่ยนแปลง
บันทึกการเปลี่ยนแปลงของโปรเจค DriveToSurviveWebApp ตั้งแต่วันที่ 11 กุมภาพันธ์ 2026 ถึงปัจจุบัน

## [เวอร์ชันปัจจุบัน] - 2026-02-18

### 🚀 ระบบตรวจสอบยืนยันตัวตนผู้ใช้และคนขับอัตโนมัติ (Automated User & Driver Verification)
*   **เพิ่มระบบตรวจสอบยืนยันคนขับอัตโนมัติ** (`driverVerification.controller.js`): พัฒนาระบบ Workflow สำหรับการยืนยันใบขับขี่แบบอัตโนมัติเต็มรูปแบบ เพื่อลดภาระงานของ Admin
    *   **การเชื่อมต่อ OCR**: เชื่อมต่อกับ `iappService` เพื่อใช้เทคโนโลยี Optical Character Recognition (OCR) อ่านข้อมูลจากรูปถ่ายใบขับขี่ที่อัปโหลดเข้ามา
    *   **ตรรกะการตรวจสอบอัจฉริยะ (Smart Validation Logic)**:
        *   ระบบจะดึงข้อมูล ชื่อ-นามสกุล, เลขที่ใบขับขี่, วันออกบัตร/วันหมดอายุ และประเภทใบขับขี่ จากรูปภาพโดยอัตโนมัติ
        *   **การตรวจสอบตัวตน (Identity Cross-Check)**: เพิ่มมาตรการความปลอดภัยเข้มงวด โดยการเปรียบเทียบ **เลขบัตรประชาชน (Citizen ID)** ที่อ่านได้จากใบขับขี่ กับ **เลขบัตรประชาชน (National ID)** ของบัญชีผู้ใช้ที่ยืนยันแล้ว หากไม่ตรงกัน ระบบจะปฏิเสธการยืนยันทันที (`HTTP 403`) เพื่อป้องกันการสวมสิทธิ์
    *   **กระบวนการอนุมัติอัตโนมัติ (Auto-Approval Workflow)**: หากการทำ OCR สำเร็จและข้อมูลถูกต้องครบถ้วน ระบบจะเปลี่ยนสถานะการยืนยันเป็น `APPROVED` และระบุสถานะผู้ใช้เป็น `verifiedByOcr: true` โดยไม่ต้องรอ Admin กดอนุมัติ
    *   **ระบบแจ้งเตือน**: เชื่อมต่อกับ `notification.service` เพื่อส่งผลการยืนยัน (ผ่าน/ไม่ผ่าน) ให้ผู้ใช้ทราบทันทีหลังจากกระบวนการ OCR เสร็จสิ้น

### 🛡️ ความปลอดภัยและการปฏิบัติตามกฎหมาย (Blacklist System)
*   **เพิ่มกลไกบัญชีดำ (Blacklist Mechanism)** (`blacklist.service.js`): พัฒนาระบบความปลอดภัยเชิงป้องกัน เพื่อสกัดกั้นผู้ใช้ที่ถูกแบนไม่ให้กลับมาลงทะเบียนใหม่
    *   **ปฏิบัติตาม PDPA (มาตรา 22 - Data Minimization)**:
        *   ระบบ **ไม่จัดเก็บ** เลขบัตรประชาชนตัวจริง (Raw National ID) ของผู้ที่ติด Blacklist เพื่อลดความเสี่ยงด้านความเป็นส่วนตัว
        *   ใช้วิธีสร้างและจัดเก็บในรูปแบบ **SHA-256 Hash** ของเลขบัตรประชาชน (`nationalIdHash`) ลงในตาราง `Blacklist` แทน
    *   **การตรวจสอบก่อนลงทะเบียน (Pre-registration Check)**: ปรับปรุง service `createUser` (`user.service.js`) ให้ตรวจสอบเลขบัตรประชาชนของผู้สมัครเทียบกับ Blacklist Hash ก่อนสร้างบัญชี
        *   หากพบข้อมูลตรงกัน การลงทะเบียนจะถูกบล็อกทันที พร้อมส่ง error `403 Forbidden` ("This National ID has been blacklisted")
    *   **ระบบจัดการสำหรับ Admin**: เพิ่ม API Endpoints สำหรับ Admin เพื่อบริหารจัดการ Blacklist (เพิ่ม/ลบ/ค้นหา) พร้อมบันทึกเหตุผลประกอบ

### ⚖️ การปฏิบัติตามกฎหมาย (System Logs)
*   **เพิ่มการจัดเก็บข้อมูลจราจรคอมพิวเตอร์ (Traffic Data Logging)** (`systemLog.middleware.js`): พัฒนาระบบ Logging ศูนย์กลางเพื่อให้เป็นไปตาม **พ.ร.บ.คอมพิวเตอร์ฯ พ.ศ. 2560 มาตรา 26**
    *   **การเก็บรักษาข้อมูล**: ระบบจะดักจับและบันทึกข้อมูลจราจรของทุก API Request (`/api/*`)
    *   **ข้อมูลที่บันทึก (Captured Metrics)**:
        *   **การระบุตัวตน**: บันทึก `userId` (กรณีมีการ login)
        *   **IP Address**: พัฒนาตรรกะเพื่อดึง Client IP ที่แท้จริง โดยรองรับการอ่านค่าจาก `x-forwarded-for` headers กรณีผ่าน Proxy หรือ Load Balancer
        *   **รายละเอียดการเข้าถึง**: บันทึก `HTTP Method` (Action), `Request URL` (Resource), และ `User-Agent`
    *   **ความมั่นคงปลอดภัยของข้อมูล (Immutability)**: ออกแบบ Model และ Service ของ `SystemLog` ให้เป็นแบบ **Append-Only** (เพิ่มได้อย่างเดียว) โดย **ไม่มี** API endpoint สำหรับแก้ไข (Update) หรือลบ (Delete) ข้อมูล Log เพื่อรับประกันความถูกต้องของพยานหลักฐานทางดิจิทัล

### 🗑️ ความเป็นส่วนตัวและสิทธิในการลบข้อมูล (Account Deletion)
*   **เพิ่มระบบลบบัญชีผู้ใช้ / สิทธิในการถูกลืม** (`user.service.js`): พัฒนากลไก "Soft Delete" ให้สอดคล้องกับ **PDPA มาตรา 33 (Right to Erasure)**
    *   **การทำให้เป็นข้อมูลนิรนาม (PII Anonymization)**: เมื่อผู้ใช้แจ้งลบบัญชี (หรือถูก Admin ลบ) ระบบจะไม่เพียงแค่ซ่อนข้อมูล แต่จะทำการแก้ไขข้อมูลส่วนบุคคล (PII) ให้ระบุตัวตนไม่ได้:
        *   `firstName` / `lastName` -> เปลี่ยนเป็น "Deleted User"
        *   `email` -> สุ่มเปลี่ยนเป็น `deleted_{id}@removed.local`
        *   `username` -> สุ่มเปลี่ยนเป็น `deleted_{id}`
        *   `phoneNumber`, `profilePicture`, `nationalIdPhotoUrl` -> เปลี่ยนเป็น `NULL`
        *   `isActive` -> เปลี่ยนเป็น `false`
    *   **การจัดการความขัดแย้งทางกฎหมาย (Legal Conflict Resolution)**:
        *   การออกแบบคำนึงถึงความสมดุลระหว่าง **PDPA (ลบข้อมูลส่วนบุคคล)** และ **พ.ร.บ.คอมพิวเตอร์ฯ (เก็บ Log 90 วัน)**
        *   ระบบจะยังคงรักษา `User.id` ไว้ในฐานข้อมูล (เพื่อให้ `SystemLog` ยังสามารถอ้างอิงได้ครบระยะเวลา 90 วัน) แต่เนื้อหาในโปรไฟล์ผู้ใช้จะถูกล้างข้อมูลส่วนบุคคลออกทั้งหมดแล้ว

### 🔑 ระบบกู้คืนรหัสผ่าน (Forgot Password)
*   **เพิ่มฟีเจอร์ลืมรหัสผ่าน** (`auth.controller.js`): พัฒนาระบบกู้คืนรหัสผ่านด้วย OTP ผ่านอีเมล เพื่อความปลอดภัยและสะดวกสบายของผู้ใช้
    *   **การขอรหัส OTP**: ผู้ใช้สามารถขอรหัส OTP ได้ผ่าน Endpoint `/api/auth/forgot-password` ระบบจะส่งรหัส 6 หลักไปยังอีเมลที่ลงทะเบียนไว้
    *   **ความปลอดภัย**:
        *   **ป้องกันการสุ่มอีเมล (Enumeration Protection)**: API จะตอบกลับด้วยข้อความ Success เสมอ ไม่ว่าอีเมลจะมีในระบบหรือไม่ เพื่อป้องกันผู้ไม่หวังดีตรวจสอบรายชื่อผู้ใช้
        *   **อายุของ OTP**: รหัส OTP มีอายุใช้งานเพียง 5 นาที เพื่อความปลอดภัย
    *   **กระบวนการรีเซ็ต**: รองรับการตรวจสอบความถูกต้องของ OTP (`/api/auth/verify-otp`) และการตั้งรหัสผ่านใหม่ (`/api/auth/reset-password`) เมื่อยืนยันตัวตนสำเร็จ

## [Current Working State] - 2026-02-16

### 🔧 System & Infrastructure / ระบบและโครงสร้าง
- **Major Restructuring / ปรับโครงสร้างใหญ่**:
  - รวมโปรเจค Frontend (Nuxt) และ Backend (Express) เข้าด้วยกันเพื่อความสะดวกในการพัฒนา
  - ย้ายโค้ดไปยังโฟลเดอร์ `client/` และ `server/` เพื่อความเป็นระเบียบ
  - ลบไฟล์ config เก่าที่ซ้ำซ้อนออกจาก root directory
- **Environment**:
  - แก้ไขปัญหาการโหลด Environment Variables (.env)
  - แก้ไขปัญหา Port conflicts ระหว่าง services

## 2026-02-14

### 🐛 Bug Fixes / การแก้ไขบั๊ก
- **Development Environment**:
  - แก้ไขปัญหา JWT Token หมดอายุ (Expiration issues)
  - แก้ไข Nuxt build errors ที่ขัดขวางการรัน dev server

## 2026-02-13

### ✨ Features & Improvements / ฟีเจอร์และการปรับปรุง
- **Deep Integration**:
  - เชื่อมต่อ Express backend เข้ากับ Nuxt frontend (Server Middleware / Server Routes)
  - รวม Dependencies ให้สามารถติดตั้งและรันได้จากคำสั่งเดียว
- **Data Security**:
  - ตรวจสอบโครงสร้างการจัดเก็บข้อมูลเลขบัตรประชาชน (National ID) ใน Schema (Confirmed storage as String)

## 2026-02-12

### ⚖️ Legal & Compliance / กฎหมายและข้อกำหนด
- **Legal Pages**:
  - สร้างหน้า Terms of Service (ข้อกำหนดการใช้งาน)
  - สร้างหน้า Privacy Policy (นโยบายความเป็นส่วนตัว)
  - อัปเดตเนื้อหาให้ถูกต้องตามชื่อโปรเจค "Drive To Survive"
- **User Registration**:
  - เพิ่ม Checkbox "Accept Terms & Policy" ในแบบฟอร์มลงทะเบียนบังคับให้ผู้ใช้ยอมรับก่อนสมัคร


